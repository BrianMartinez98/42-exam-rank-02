# Maintenance notes

Clean-up performed for GitHub presentation:

- Removed folders without source files from the published copy.
- Added `.gitignore` and `scripts/check.sh`.
- Fixed compilation issues caused by missing standard headers.
- Fixed obvious memory/allocation issues in `ft_range`, `ft_rrange`, `ft_itoa`, `ft_strdup`, and `ft_split`.
- Fixed boundary errors in `first_word`, `rev_print`, `max`, and `sort_int_tab`.
- Fixed `ft_strcspn` implementation to match the standard behavior.
